<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.youtube.iframeplayer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name="com.youtube.download.DownloadActivity"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/YouTubeAppTheme" />

        <activity
            android:name="com.youtube.download.AdvancedDownloadActivity"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/YouTubeAppTheme" />

        <receiver
            android:name="com.youtube.download.DownloadFinishedReceiver"
            android:exported="true"
            android:process=".downloadFinished">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
